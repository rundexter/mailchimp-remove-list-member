/*
 * Metadata describing the module operation
 */
{
    //Required. Inputs the module expects.
    "inputs": [
        //e.g.
        {
            "id": "list_id",
            "title":"List ID"
        },
        {
            "id": "subscriber_hash",
            "title":"Subscriber Hash"
        }
    ],
    //Required. Outputs the module emits.
    "outputs": [
      //e.g.
        {
            "id": "success",
            "title":"Success"
        }
    ],
    "providers": [{
        "name": "mailchimp "
    }],
    "icon": "dex-mailchimp",
    "title": "Mailchimp Remove List Member" /* Required.A display name for the module */
}
